# TODO

This file tracks concrete remaining work visible in the repo as of `7f98eed`.
It is intentionally limited to explicit gaps, known issues, and source-level TODOs.

## High Priority

- [x] Remove the placeholder briefing text from `server/distill.go`'s `distillSystemPrompt`.
  The prompt still literally says `WRITE 2-6 SENTENCES HERE...`; replace that with the final instruction wording only.

- [x] Eliminate the React `act(...)` warnings from the hook tests in `ui/src/test/hookTestUtils.tsx`.
  The current lightweight harness passes, but async state updates are still escaping the helper.

- [x] Add direct tests for the new stream/snapshot hooks.
  `useConversationSnapshot.ts`, `useConversationStream.ts`, and `useConversationSession.ts` now own most transport/state behavior, but only the composer/actions/coalescing hooks have focused tests.

## Frontend Follow-Up After Phase 4

- [x] Remove `ui/src/hooks/useConversationSession.ts` and compose the session hooks directly in `ChatInterface`.
  The wrapper is gone; `ChatInterface` now wires snapshot loading, stream lifecycle, and actions directly.

- [ ] Verify the extracted stream hooks against a live browser session.
  The reconnect spam is fixed in code, but the intended manual follow-up is a real `make serve` session to confirm stable SSE behavior, reconnects, and resume behavior.

- [ ] Consider replacing the custom hook runner with a real React test stack.
  The current `tsx` + `jsdom` harness is small, but it is already carrying React-specific act/environment setup by hand.

## Known Product Issues From `AGENT_TESTING.md`

- [ ] Re-check the rapid sequential message bug that can produce `400` errors for empty content.

- [ ] Re-check cancelled-operation reload behavior so cancelled turns render explicit cancelled state after refresh.

- [ ] Re-check that the "Agent working..." indicator clears on LLM errors.

- [ ] Re-check mobile drawer/menu viewport behavior.

- [ ] Keep the React-compatible textarea setter requirement documented anywhere browser automation is added.

## Server And Stream Backlog

- [ ] Consider gzip support for SSE in `server/handlers.go`.

- [ ] Evaluate whether `server/server.go` can omit `llm_data` when `display_data` is available.

- [ ] Revisit the distillation request in `server/distill.go` to disable thinking if that meaningfully reduces token cost without hurting output quality.

## Database Backlog

- [ ] Harden `db/pool.go` against callers overriding `PRAGMA query_only`.

- [ ] Confirm the SQLite driver compatibility assumptions in `db/pool.go`.

## Loop / Conversation Model Backlog

- [ ] Decide whether `loop/Loop` should be renamed to better match turn-scoped behavior (`loop/loop.go`).

- [ ] Reduce Anthropic-specific leakage from `llm/conversation/convo.go`'s content model.

- [ ] Decide where start/end time and usage should live in the conversation model.

- [ ] Add finer-grained control over tool use in `llm/conversation/convo.go`.

- [ ] Document and tighten parent/subagent budget behavior in `llm/conversation/convo.go`.

- [ ] Stop before exceeding conversation budgets instead of after in `llm/conversation/convo.go`.

## Provider Backlog

- [ ] Determine real Gemini image dimension limits in `llm/gem/gem.go` and `llm/gem/gemini/gemini.go`.

- [ ] Add `inlineData`, `fileData`, and possibly `googleSearchRetrieval` support in `llm/gem/gemini/gemini.go`.

- [ ] Replace the remote Gemini test dependency with a local replay endpoint in `llm/gem/gemini/gemini_test.go`.

- [ ] Determine real OpenAI image dimension limits in `llm/oai/oai.go` and `llm/oai/oai_responses.go`.

- [ ] Revisit `fromLLMToolChoice` error handling in `llm/oai/oai.go` so lossy translations fail explicitly.

- [ ] Consider moving the provider initialization noted in `llm/oai/oai.go` into service setup.

## Tooling Backlog

- [ ] Cache the shell resolution result with `sync.OnceValue` in `claudetool/bash.go`.

- [ ] Fix zombie process behavior for bash invocations when Shelley is PID 1 in `claudetool/bash.go`.

- [ ] Decide whether `claudetool/onstart/analyze.go` should switch to a filesystem walk.

- [ ] Rename `PatchRequest` to something clearer in `claudetool/patch.go`.

- [ ] Tighten autogenerated-file handling in `claudetool/patch.go` so edits require explicit force.

- [ ] Decide whether patch application should partially apply clean hunks or stay all-or-nothing in `claudetool/patch.go`.

- [ ] Improve recovery when patch application enters a repeated failure loop in `claudetool/patch.go`.

- [ ] Continue the performance cleanup noted across `claudetool/patchkit/patchkit.go`.

## Test / Fixture Cleanup

- [ ] Add actual tools to the integration fixture in `loop/integration_test.go` if coverage needs them.

- [ ] Keep the predictable model fixture aligned with any tool/UI changes.

## Documentation

- [ ] Add or update a brief architecture note for the post-Phase-4 frontend split.
  The main boundaries are now `useComposerPreferences`, `useConversationSnapshot`, `useConversationStream`, `useConversationActions`, `useMessageScroll`, and the extracted status/overflow/coalescing UI pieces.
